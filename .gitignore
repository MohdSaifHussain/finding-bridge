# Evidence PDFs are pointed to from the charter's source index, not committed
# (ruling D-006). Re-fetch from the arXiv URL named there if lost.
docs/FLARE AI Flaw Reporting for AI.pdf

# Local finding workspace (sealed blobs, candidates, ledger): never committed
.fb-store/

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/

# Mutation-audit session databases (evidence lives in evidence/, not binaries)
cr-*.sqlite

# Build artifacts (W2c proof rebuilds them on demand)
build/
dist/
*.whl
