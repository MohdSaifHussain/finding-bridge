---
name: Finding (bug, mis-parse, wrong refusal)
about: Report something the tool did wrong, with the reason code and the location
title: "F: <one line>"
labels: finding
---

**Reason code and location** (as the tool printed them; never the value):

**Command run** (exact, with flags):

**Expected** versus **observed** (exit code and output):

**Source shape** (garak version and record shape, or transcript grammar),
without any harmful content. Describe the shape; do not paste model
output. Fixtures are synthetic with `SENTINEL-*` stand-ins.

**Version** (`finding-bridge --help` header, or the image tag):
